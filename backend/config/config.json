{
    "database" : {
        "user" : "{username}",
        "password" : "{password}",
        "database_name" : "{db name}"
    },
    "bot_token" : "{bot-token}",
    "weatherApiKey" : "{weather-token}"
}
